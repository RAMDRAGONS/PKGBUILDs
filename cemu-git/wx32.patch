diff --git i/CMakeLists.txt w/CMakeLists.txt
index 69a5b0b0..b4bd2456 100644
--- i/CMakeLists.txt
+++ w/CMakeLists.txt
@@ -73,8 +73,6 @@ add_definitions(-DEMULATOR_VERSION_PATCH=${EMULATOR_VERSION_PATCH})
 set_property(GLOBAL PROPERTY USE_FOLDERS ON)
 
 # enable link time optimization for release builds
-set(CMAKE_INTERPROCEDURAL_OPTIMIZATION_RELEASE ON)
-set(CMAKE_INTERPROCEDURAL_OPTIMIZATION_RELWITHDEBINFO ON)
 
 if (MSVC)
 	set_property(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT CemuBin)
@@ -233,7 +231,7 @@ if(UNIX AND NOT APPLE)
 endif()
 
 if (ENABLE_WXWIDGETS)
-	find_package(wxWidgets 3.3 REQUIRED COMPONENTS base core gl propgrid xrc)
+	find_package(wxWidgets 3.2 REQUIRED COMPONENTS base core gl propgrid xrc)
 endif()
 
 if (ENABLE_CUBEB)
diff --git i/src/CMakeLists.txt w/src/CMakeLists.txt
index 977f48b7..b3a39844 100644
--- i/src/CMakeLists.txt
+++ w/src/CMakeLists.txt
@@ -211,6 +211,10 @@ set_target_properties(CemuBin PROPERTIES
 	OUTPUT_NAME "${OUTPUT_NAME}"
 )
 
+if(ENABLE_SDL)
+	target_link_libraries(CemuBin PRIVATE SDL3::SDL3)
+endif()
+
 target_link_libraries(CemuBin PRIVATE
 	CemuAudio
 	CemuCafe
@@ -222,9 +226,6 @@ target_link_libraries(CemuBin PRIVATE
 	CemuUtil
 )
 
-if(ENABLE_SDL)
-	target_link_libraries(CemuBin PRIVATE SDL3::SDL3)
-endif()
 
 if(UNIX AND NOT APPLE)
 	# due to nasm output some linkers will make stack executable
diff --git i/src/Common/precompiled.h w/src/Common/precompiled.h
index e0373952..8560ca3c 100644
--- i/src/Common/precompiled.h
+++ w/src/Common/precompiled.h
@@ -5,7 +5,6 @@
 #include "version.h"
 #include "platform.h"
 
-#define FMT_HEADER_ONLY
 #define FMT_USE_GRISU 1
 #include <fmt/core.h>
 #include <fmt/format.h>
diff --git i/src/gui/wxgui/CemuApp.cpp w/src/gui/wxgui/CemuApp.cpp
index 9a636989..69ee55fe 100644
--- i/src/gui/wxgui/CemuApp.cpp
+++ w/src/gui/wxgui/CemuApp.cpp
@@ -257,7 +257,7 @@ void CemuApp::InitializeExistingMLCOrFail(fs::path mlc)
 
 std::string TranslationCallback(std::string_view msgId)
 {
-	return wxGetTranslation(wxString::FromUTF8(msgId)).utf8_string();
+	return wxGetTranslation(wxString::FromUTF8(msgId.data(), msgId.size())).utf8_string();
 }
 
 bool CemuApp::OnInit()
diff --git i/src/gui/wxgui/GraphicPacksWindow2.cpp w/src/gui/wxgui/GraphicPacksWindow2.cpp
index 9ac52304..9d53dfe6 100644
--- i/src/gui/wxgui/GraphicPacksWindow2.cpp
+++ w/src/gui/wxgui/GraphicPacksWindow2.cpp
@@ -96,10 +96,10 @@ void GraphicPacksWindow2::FillGraphicPackList() const
 			const auto parent_node = node;
 			if (i < (tokens.size() - 1))
 			{
-				node = FindTreeItem(parent_node, wxString::FromUTF8(token));
+				node = FindTreeItem(parent_node, wxString::FromUTF8(token.data(), token.size()));
 				if (!node.IsOk())
 				{
-					node = m_graphic_pack_tree->AppendItem(parent_node, wxString::FromUTF8(token));
+					node = m_graphic_pack_tree->AppendItem(parent_node, wxString::FromUTF8(token.data(), token.size()));
 				}
 			}
 			else
@@ -108,7 +108,7 @@ void GraphicPacksWindow2::FillGraphicPackList() const
 				// if a node with same name already exists, add a number suffix
 				for (sint32 s = 0; s < 999; s++)
 				{
-					wxString nodeName = wxString::FromUTF8(token);
+					wxString nodeName = wxString::FromUTF8(token.data(), token.size());
 					if (s > 0)
 						nodeName.append(formatWxString(" #{}", s + 1));
 
diff --git i/src/gui/wxgui/GraphicPacksWindow2.h w/src/gui/wxgui/GraphicPacksWindow2.h
index 0d24e87b..78be5766 100644
--- i/src/gui/wxgui/GraphicPacksWindow2.h
+++ w/src/gui/wxgui/GraphicPacksWindow2.h
@@ -8,6 +8,7 @@
 #include "wxcomponents/checktree.h"
 
 #include "Cafe/GraphicPack/GraphicPack2.h"
+#include "wxHelper.h"
 
 class wxSplitterWindow;
 class wxPanel;
@@ -55,8 +56,8 @@ private:
 
 	float m_ratio = 0.55f;
 	wxColour m_default_colour = wxSystemSettings::GetColour(wxSYS_COLOUR_WINDOWTEXT);
-	wxColour m_activated_colour = wxSystemSettings::SelectLightDark(wxColour(0x00, 0xCC, 0x00), wxColour(0x42, 0xB3, 0x42));
-	wxColour m_incompatible_colour = wxSystemSettings::SelectLightDark(wxColour(0xCC, 0x00, 0x00), wxColour(0xDE, 0x49, 0x49));
+	wxColour m_activated_colour = wxHelper::SelectLightDark(wxColour(0x00, 0xCC, 0x00), wxColour(0x42, 0xB3, 0x42));
+	wxColour m_incompatible_colour = wxHelper::SelectLightDark(wxColour(0xCC, 0x00, 0x00), wxColour(0xDE, 0x49, 0x49));
 
 	wxTreeItemId FindTreeItem(const wxTreeItemId& root, const wxString& text) const;
 	void LoadPresetSelections(const GraphicPackPtr& gp);
diff --git i/src/gui/wxgui/MainWindow.cpp w/src/gui/wxgui/MainWindow.cpp
index cac5db85..9f3a7ebc 100644
--- i/src/gui/wxgui/MainWindow.cpp
+++ w/src/gui/wxgui/MainWindow.cpp
@@ -1587,7 +1587,7 @@ void MainWindow::OnGameLoaded()
 void MainWindow::AsyncSetTitle(std::string_view windowTitle)
 {
 	wxCommandEvent set_title_event(wxEVT_SET_WINDOW_TITLE);
-	set_title_event.SetString(wxString::FromUTF8(windowTitle));
+	set_title_event.SetString(wxString::FromUTF8(windowTitle.data(), windowTitle.size()));
 	g_mainFrame->QueueEvent(set_title_event.Clone());
 }
 
diff --git i/src/gui/wxgui/PadViewFrame.cpp w/src/gui/wxgui/PadViewFrame.cpp
index ca213849..6ffaa4f8 100644
--- i/src/gui/wxgui/PadViewFrame.cpp
+++ w/src/gui/wxgui/PadViewFrame.cpp
@@ -237,6 +237,6 @@ void PadViewFrame::OnSetWindowTitle(wxCommandEvent& event)
 void PadViewFrame::AsyncSetTitle(std::string_view windowTitle)
 {
 	wxCommandEvent set_title_event(wxEVT_SET_WINDOW_TITLE);
-	set_title_event.SetString(wxString::FromUTF8(windowTitle));
+	set_title_event.SetString(wxString::FromUTF8(windowTitle.data(), windowTitle.size()));
 	QueueEvent(set_title_event.Clone());
 }
diff --git i/src/gui/wxgui/TitleManager.cpp w/src/gui/wxgui/TitleManager.cpp
index a4e191c9..4dbf9341 100644
--- i/src/gui/wxgui/TitleManager.cpp
+++ w/src/gui/wxgui/TitleManager.cpp
@@ -382,7 +382,7 @@ void TitleManager::OnInstallTitle(wxCommandEvent& event)
 	if (openFileDialog.ShowModal() == wxID_CANCEL || openFileDialog.GetPath().IsEmpty())
 		return;
 
-	fs::path filePath(openFileDialog.GetPath().wc_string());
+	fs::path filePath(openFileDialog.GetPath().wc_str());
 	try
 	{
 		filePath = filePath.parent_path();
diff --git i/src/gui/wxgui/components/TextList.cpp w/src/gui/wxgui/components/TextList.cpp
index f5e3f133..92c80508 100644
--- i/src/gui/wxgui/components/TextList.cpp
+++ w/src/gui/wxgui/components/TextList.cpp
@@ -27,8 +27,12 @@ TextList::TextList(wxWindow* parent, wxWindowID id, const wxPoint& pos, const wx
 	m_font = wxSystemSettings::GetFont(wxSYS_DEFAULT_GUI_FONT);
 	this->wxWindowBase::SetBackgroundStyle(wxBG_STYLE_PAINT);
 
+	#if wxCHECK_VERSION(3, 3, 0)
 	wxInfoDC dc(this);
 	this->DoPrepareReadOnlyDC(dc);
+#else
+	wxClientDC dc(this);
+#endif
 	dc.SetFont(m_font);
 	m_line_height = dc.GetCharHeight();
 	m_char_width = dc.GetCharWidth();
diff --git i/src/gui/wxgui/components/wxGameList.h w/src/gui/wxgui/components/wxGameList.h
index 388a9df6..bc937149 100644
--- i/src/gui/wxgui/components/wxGameList.h
+++ w/src/gui/wxgui/components/wxGameList.h
@@ -63,8 +63,8 @@ private:
 	Style m_style;
 	long GetStyleFlags(Style style) const;
 
-	const wxColour kUpdateColor{ wxSystemSettings::SelectLightDark(wxColour(195, 57, 57), wxColour(84, 29, 29)) };
-	const wxColour kFavoriteColor{ wxSystemSettings::SelectLightDark(wxColour(253, 246, 211), wxColour(82, 84, 48)) };
+	const wxColour kUpdateColor{ wxHelper::SelectLightDark(wxColour(195, 57, 57), wxColour(84, 29, 29)) };
+	const wxColour kFavoriteColor{ wxHelper::SelectLightDark(wxColour(253, 246, 211), wxColour(82, 84, 48)) };
 	const wxColour kPrimaryColor = GetBackgroundColour();
 	const wxColour kAlternateColor = wxHelper::CalculateAccentColour(kPrimaryColor);
 	void UpdateItemColors(sint32 startIndex = 0);
diff --git i/src/gui/wxgui/components/wxInputDraw.cpp w/src/gui/wxgui/components/wxInputDraw.cpp
index d12237c8..68f8652d 100644
--- i/src/gui/wxgui/components/wxInputDraw.cpp
+++ w/src/gui/wxgui/components/wxInputDraw.cpp
@@ -1,4 +1,5 @@
 #include "wxgui/components/wxInputDraw.h"
+#include "wxHelper.h"
 
 #include <wx/dcbuffer.h>
 #include <wx/settings.h>
@@ -27,12 +28,12 @@ void wxInputDraw::OnRender(wxDC& dc)
 	wxPen black = wxPen(wxSystemSettings::GetColour(wxSYS_COLOUR_WINDOWTEXT));
 	wxPen red = *wxRED_PEN;
 	wxPen grey = wxPen(wxSystemSettings::GetColour(wxSYS_COLOUR_GRAYTEXT));
-	wxPen green = wxSystemSettings::SelectLightDark(0x336600, 0x99FF99);
+	wxPen green = wxHelper::SelectLightDark(wxColour(0x336600), wxColour(0x99FF99));
 
 	wxBrush black_brush = wxSystemSettings::GetColour(wxSYS_COLOUR_WINDOWTEXT);
 	wxBrush red_brush = *wxRED_BRUSH;
 	wxBrush grey_brush = wxSystemSettings::GetColour(wxSYS_COLOUR_GRAYTEXT);
-	wxBrush green_brush = wxSystemSettings::SelectLightDark(0x336600, 0x99FF99);
+	wxBrush green_brush = wxHelper::SelectLightDark(wxColour(0x336600), wxColour(0x99FF99));
 
 	if(!IsEnabled())
 	{
diff --git i/src/gui/wxgui/debugger/DisasmCtrl.cpp w/src/gui/wxgui/debugger/DisasmCtrl.cpp
index 33da4774..c3d6769c 100644
--- i/src/gui/wxgui/debugger/DisasmCtrl.cpp
+++ w/src/gui/wxgui/debugger/DisasmCtrl.cpp
@@ -72,32 +72,32 @@ static wxColour theme_patchedData;
 static void InitSyntaxColors()
 {
 	theme_textForeground = wxSystemSettings::GetColour(wxSYS_COLOUR_WINDOWTEXT);
-	theme_textForegroundMuted = wxSystemSettings::SelectLightDark(wxSystemSettings::GetColour(wxSYS_COLOUR_GRAYTEXT), wxColour(140, 142, 145));
+	theme_textForegroundMuted = wxHelper::SelectLightDark(wxSystemSettings::GetColour(wxSYS_COLOUR_GRAYTEXT), wxColour(140, 142, 145));
 	theme_background = wxSystemSettings::GetColour(wxSYS_COLOUR_WINDOW);
 
 	// line background highlights
-	theme_lineBreakpointSet = wxSystemSettings::SelectLightDark(wxColour(0xFF,0x80,0x80), wxColour(0x84,0x21,0x21)); // red for a non-current breakpoint
-	theme_lineBreakpointAndCurrentInstruction = wxSystemSettings::SelectLightDark(wxColour(0xD2,0x7E,0xD2), wxColour(0x91,0x44,0xA4)); // pink for a current breakpoint
-	theme_lineCurrentInstruction = wxSystemSettings::SelectLightDark(wxColour(0xFF,0xA0,0x80), wxColour(0x87,0x53,0x1A)); // light orange
-	theme_lineLoggingBreakpointSet = wxSystemSettings::SelectLightDark(wxColour(0xAB,0xED,0xEE), wxColour(0x25,0x5D,0x6D)); // light blue
-	theme_lineLastGotoAddress = wxSystemSettings::SelectLightDark(wxColour(0xE0,0xE0,0xE0), wxColour(0x40,0x40,0x40)); // very light gray to indicate the line that was last jumped to
+	theme_lineBreakpointSet = wxHelper::SelectLightDark(wxColour(0xFF,0x80,0x80), wxColour(0x84,0x21,0x21)); // red for a non-current breakpoint
+	theme_lineBreakpointAndCurrentInstruction = wxHelper::SelectLightDark(wxColour(0xD2,0x7E,0xD2), wxColour(0x91,0x44,0xA4)); // pink for a current breakpoint
+	theme_lineCurrentInstruction = wxHelper::SelectLightDark(wxColour(0xFF,0xA0,0x80), wxColour(0x87,0x53,0x1A)); // light orange
+	theme_lineLoggingBreakpointSet = wxHelper::SelectLightDark(wxColour(0xAB,0xED,0xEE), wxColour(0x25,0x5D,0x6D)); // light blue
+	theme_lineLastGotoAddress = wxHelper::SelectLightDark(wxColour(0xE0,0xE0,0xE0), wxColour(0x40,0x40,0x40)); // very light gray to indicate the line that was last jumped to
 
 	// disassembly syntax colors
-	theme_syntaxGPR = wxSystemSettings::SelectLightDark(wxColour(0x66,0x00,0x00), wxColour(0xE0,0x6C,0x75));
-	theme_syntaxFPR = wxSystemSettings::SelectLightDark(wxColour(0x66,0x66,0x00), wxColour(0xD1,0x9A,0x66));
-	theme_syntaxSPR = wxSystemSettings::SelectLightDark(wxColour(0x00,0x66,0x66), wxColour(0x56,0xB6,0xC2));
-	theme_syntaxCR = wxSystemSettings::SelectLightDark(wxColour(0x00,0x66,0x66), wxColour(0x56,0xB6,0xC2));
-	theme_syntaxIMM = wxSystemSettings::SelectLightDark(wxColour(0x00,0x66,0x00), wxColour(0x9D,0xDE,0x6F));
-	theme_syntaxIMMOffset = wxSystemSettings::SelectLightDark(wxColour(0x00,0x66,0x00), wxColour(0x9D,0xDE,0x6F));
-	theme_syntaxCallIMM = wxSystemSettings::SelectLightDark(wxColour(0x00,0x00,0x88), wxColour(0x61,0xAF,0xEF));
-	theme_syntaxPseudoOrUnknown = wxSystemSettings::SelectLightDark(wxColour(0xA0,0xA0,0xA0), wxColour(0x5C,0x63,0x70));
-	theme_syntaxSymbol = wxSystemSettings::SelectLightDark(wxColour(0xA0,0x00,0x00), wxColour(0xE0,0x6C,0x75));
+	theme_syntaxGPR = wxHelper::SelectLightDark(wxColour(0x66,0x00,0x00), wxColour(0xE0,0x6C,0x75));
+	theme_syntaxFPR = wxHelper::SelectLightDark(wxColour(0x66,0x66,0x00), wxColour(0xD1,0x9A,0x66));
+	theme_syntaxSPR = wxHelper::SelectLightDark(wxColour(0x00,0x66,0x66), wxColour(0x56,0xB6,0xC2));
+	theme_syntaxCR = wxHelper::SelectLightDark(wxColour(0x00,0x66,0x66), wxColour(0x56,0xB6,0xC2));
+	theme_syntaxIMM = wxHelper::SelectLightDark(wxColour(0x00,0x66,0x00), wxColour(0x9D,0xDE,0x6F));
+	theme_syntaxIMMOffset = wxHelper::SelectLightDark(wxColour(0x00,0x66,0x00), wxColour(0x9D,0xDE,0x6F));
+	theme_syntaxCallIMM = wxHelper::SelectLightDark(wxColour(0x00,0x00,0x88), wxColour(0x61,0xAF,0xEF));
+	theme_syntaxPseudoOrUnknown = wxHelper::SelectLightDark(wxColour(0xA0,0xA0,0xA0), wxColour(0x5C,0x63,0x70));
+	theme_syntaxSymbol = wxHelper::SelectLightDark(wxColour(0xA0,0x00,0x00), wxColour(0xE0,0x6C,0x75));
 
 	// opcode & data highlighting
-	theme_opCode = wxSystemSettings::SelectLightDark(wxColour(0xFF,0x00,0x40), wxColour(0xFF,0x70,0x7B));
-	theme_patchedOpCode = wxSystemSettings::SelectLightDark(wxColour(0xFF,0x20,0x20), wxColour(0xCC,0x52,0xF5));
-	theme_typeData = wxSystemSettings::SelectLightDark(wxColour(0xFF,0x20,0x20), wxColour(0xCE,0x91,0x78));
-	theme_patchedData = wxSystemSettings::SelectLightDark(wxColour(0xFF,0x20,0x20), wxColour(0xF4,0x43,0x36));
+	theme_opCode = wxHelper::SelectLightDark(wxColour(0xFF,0x00,0x40), wxColour(0xFF,0x70,0x7B));
+	theme_patchedOpCode = wxHelper::SelectLightDark(wxColour(0xFF,0x20,0x20), wxColour(0xCC,0x52,0xF5));
+	theme_typeData = wxHelper::SelectLightDark(wxColour(0xFF,0x20,0x20), wxColour(0xCE,0x91,0x78));
+	theme_patchedData = wxHelper::SelectLightDark(wxColour(0xFF,0x20,0x20), wxColour(0xF4,0x43,0x36));
 
 	// theme the current instruction pointer arrow
 	g_ipArrowBitmap = wxHelper::LoadThemedBitmapFromPNG(arrowRightPNG, sizeof(arrowRightPNG), wxSystemSettings::GetColour(wxSYS_COLOUR_WINDOWTEXT));
diff --git i/src/gui/wxgui/input/InputAPIAddWindow.cpp w/src/gui/wxgui/input/InputAPIAddWindow.cpp
index b10a2c75..e931934b 100644
--- i/src/gui/wxgui/input/InputAPIAddWindow.cpp
+++ w/src/gui/wxgui/input/InputAPIAddWindow.cpp
@@ -43,7 +43,7 @@ InputAPIAddWindow::InputAPIAddWindow(wxWindow* parent, const wxPoint& position,
 				continue;
 
 			const auto provider = *p.begin();
-			m_input_api->Append(wxString::FromUTF8(provider->api_name()), new wxTypeData(provider->api()));
+			m_input_api->Append(wxString::FromUTF8(provider->api_name().data(), provider->api_name().size()), new wxTypeData(provider->api()));
 		}
 
 		m_input_api->Bind(wxEVT_CHOICE, &InputAPIAddWindow::on_api_selected, this);
@@ -150,7 +150,7 @@ void InputAPIAddWindow::on_close_button(wxCommandEvent& event)
 bool InputAPIAddWindow::has_custom_settings() const
 {
 	const auto selection = m_input_api->GetStringSelection();
-	return selection == wxString::FromUTF8(to_string(InputAPI::DSUClient));
+	return selection == wxString::FromUTF8(to_string(InputAPI::DSUClient).data(), to_string(InputAPI::DSUClient).size());
 }
 
 std::unique_ptr<ControllerProviderSettings> InputAPIAddWindow::get_settings() const
@@ -174,7 +174,7 @@ void InputAPIAddWindow::on_api_selected(wxCommandEvent& event)
 
     const auto selection = m_input_api->GetStringSelection();
 	// keyboard is a special case, as theres only one device supported atm
-	if (selection == wxString::FromUTF8(to_string(InputAPI::Keyboard)))
+	if (selection == wxString::FromUTF8(to_string(InputAPI::Keyboard).data(), to_string(InputAPI::Keyboard).size()))
 	{
 		const auto controllers = InputManager::instance().get_api_provider(InputAPI::Keyboard)->get_controllers();
 		if (!controllers.empty())
diff --git i/src/gui/wxgui/input/InputSettings2.cpp w/src/gui/wxgui/input/InputSettings2.cpp
index d77e1430..94d31b14 100644
--- i/src/gui/wxgui/input/InputSettings2.cpp
+++ w/src/gui/wxgui/input/InputSettings2.cpp
@@ -1,4 +1,5 @@
 #include "wxgui/input/InputSettings2.h"
+#include "wxHelper.h"
 
 #include <wx/gbsizer.h>
 
@@ -354,7 +355,7 @@ void InputSettings2::update_state()
 		has_controllers = !emulated_controller->get_controllers().empty();
 
 		const auto emulated_type = emulated_controller->type();
-		int index = page_data.m_emulated_controller->Append(wxString::FromUTF8(emulated_controller->type_to_string(emulated_type)));
+		int index = page_data.m_emulated_controller->Append(wxString::FromUTF8(emulated_controller->type_to_string(emulated_type).data(), emulated_controller->type_to_string(emulated_type).size()));
 		page_data.m_emulated_controller->SetSelection(index);
 
 		const auto controller_selection = page_data.m_controllers->GetStringSelection();
@@ -764,7 +765,7 @@ void InputSettings2::on_emulated_controller_dropdown(wxCommandEvent& event)
 	const auto selected_value = emulated_controllers->GetStringSelection();
 	if(selected != wxNOT_FOUND)
 	{
-		is_gamepad_selected = selected_value == wxString::FromUTF8(EmulatedController::type_to_string(EmulatedController::Type::VPAD));
+		is_gamepad_selected = selected_value == wxString::FromUTF8(EmulatedController::type_to_string(EmulatedController::Type::VPAD).data(), EmulatedController::type_to_string(EmulatedController::Type::VPAD).size());
 		is_wpad_selected = !is_gamepad_selected && selected != 0;
 	}
 
@@ -774,13 +775,13 @@ void InputSettings2::on_emulated_controller_dropdown(wxCommandEvent& event)
 	emulated_controllers->AppendString(_("Disabled"));
 
 	if (vpad_count < InputManager::kMaxVPADControllers || is_gamepad_selected)
-		emulated_controllers->Append(wxString::FromUTF8(EmulatedController::type_to_string(EmulatedController::Type::VPAD)));
+		emulated_controllers->Append(wxString::FromUTF8(EmulatedController::type_to_string(EmulatedController::Type::VPAD).data(), EmulatedController::type_to_string(EmulatedController::Type::VPAD).size()));
 
 	if (wpad_count < InputManager::kMaxWPADControllers || is_wpad_selected)
 	{
-		emulated_controllers->AppendString(wxString::FromUTF8(EmulatedController::type_to_string(EmulatedController::Type::Pro)));
-		emulated_controllers->AppendString(wxString::FromUTF8(EmulatedController::type_to_string(EmulatedController::Type::Classic)));
-		emulated_controllers->AppendString(wxString::FromUTF8(EmulatedController::type_to_string(EmulatedController::Type::Wiimote)));
+		emulated_controllers->AppendString(wxString::FromUTF8(EmulatedController::type_to_string(EmulatedController::Type::Pro).data(), EmulatedController::type_to_string(EmulatedController::Type::Pro).size()));
+		emulated_controllers->AppendString(wxString::FromUTF8(EmulatedController::type_to_string(EmulatedController::Type::Classic).data(), EmulatedController::type_to_string(EmulatedController::Type::Classic).size()));
+		emulated_controllers->AppendString(wxString::FromUTF8(EmulatedController::type_to_string(EmulatedController::Type::Wiimote).data(), EmulatedController::type_to_string(EmulatedController::Type::Wiimote).size()));
 	}
 
 	emulated_controllers->SetStringSelection(selected_value);
diff --git i/src/gui/wxgui/input/panels/ClassicControllerInputPanel.cpp w/src/gui/wxgui/input/panels/ClassicControllerInputPanel.cpp
index b1b5205c..232849e7 100644
--- i/src/gui/wxgui/input/panels/ClassicControllerInputPanel.cpp
+++ w/src/gui/wxgui/input/panels/ClassicControllerInputPanel.cpp
@@ -103,7 +103,7 @@ ClassicControllerInputPanel::ClassicControllerInputPanel(wxWindow* parent)
 
 void ClassicControllerInputPanel::add_button_row(wxGridBagSizer *sizer, sint32 row, sint32 column, const ClassicController::ButtonId &button_id) {
 	sizer->Add(
-		new wxStaticText(this, wxID_ANY, wxGetTranslation(wxString::FromUTF8(ClassicController::get_button_name(button_id)))),
+		new wxStaticText(this, wxID_ANY, wxGetTranslation(wxString::FromUTF8(ClassicController::get_button_name(button_id).data(), ClassicController::get_button_name(button_id).size()))),
 		wxGBPosition(row, column),
 		wxDefaultSpan,
 		wxALL | wxALIGN_CENTER_VERTICAL, 5);
diff --git i/src/gui/wxgui/input/panels/ProControllerInputPanel.cpp w/src/gui/wxgui/input/panels/ProControllerInputPanel.cpp
index 8b8ac741..e1733e07 100644
--- i/src/gui/wxgui/input/panels/ProControllerInputPanel.cpp
+++ w/src/gui/wxgui/input/panels/ProControllerInputPanel.cpp
@@ -101,7 +101,7 @@ ProControllerInputPanel::ProControllerInputPanel(wxWindow* parent)
 
 void ProControllerInputPanel::add_button_row(wxGridBagSizer *sizer, sint32 row, sint32 column, const ProController::ButtonId &button_id) {
 	sizer->Add(
-		new wxStaticText(this, wxID_ANY, wxGetTranslation(wxString::FromUTF8(ProController::get_button_name(button_id)))),
+		new wxStaticText(this, wxID_ANY, wxGetTranslation(wxString::FromUTF8(ProController::get_button_name(button_id).data(), ProController::get_button_name(button_id).size()))),
 		wxGBPosition(row, column),
 		wxDefaultSpan,
 		wxALL | wxALIGN_CENTER_VERTICAL, 5);
diff --git i/src/gui/wxgui/input/panels/VPADInputPanel.cpp w/src/gui/wxgui/input/panels/VPADInputPanel.cpp
index 676d70eb..b6cd638a 100644
--- i/src/gui/wxgui/input/panels/VPADInputPanel.cpp
+++ w/src/gui/wxgui/input/panels/VPADInputPanel.cpp
@@ -157,7 +157,7 @@ VPADInputPanel::VPADInputPanel(wxWindow* parent)
 }
 
 void VPADInputPanel::add_button_row(wxGridBagSizer *sizer, sint32 row, sint32 column, const VPADController::ButtonId &button_id) {
-	add_button_row(sizer, row, column, button_id, wxGetTranslation(wxString::FromUTF8(VPADController::get_button_name(button_id))));
+	add_button_row(sizer, row, column, button_id, wxGetTranslation(wxString::FromUTF8(VPADController::get_button_name(button_id).data(), VPADController::get_button_name(button_id).size())));
 }
 
 void VPADInputPanel::add_button_row(wxGridBagSizer *sizer, sint32 row, sint32 column,
diff --git i/src/gui/wxgui/input/panels/WiimoteInputPanel.cpp w/src/gui/wxgui/input/panels/WiimoteInputPanel.cpp
index da94852c..728906cb 100644
--- i/src/gui/wxgui/input/panels/WiimoteInputPanel.cpp
+++ w/src/gui/wxgui/input/panels/WiimoteInputPanel.cpp
@@ -122,7 +122,7 @@ WiimoteInputPanel::WiimoteInputPanel(wxWindow* parent)
 			continue;
 
 		m_item_sizer->Add(
-			new wxStaticText(this, wxID_ANY, wxGetTranslation(wxString::FromUTF8(WiimoteController::get_button_name(id)))),
+			new wxStaticText(this, wxID_ANY, wxGetTranslation(wxString::FromUTF8(WiimoteController::get_button_name(id).data(), WiimoteController::get_button_name(id).size()))),
 			wxGBPosition(row, column),
 			wxDefaultSpan,
 			wxALL | wxALIGN_CENTER_VERTICAL, 5);
@@ -158,7 +158,7 @@ WiimoteInputPanel::WiimoteInputPanel(wxWindow* parent)
 
 void WiimoteInputPanel::add_button_row(sint32 row, sint32 column, const WiimoteController::ButtonId &button_id) {
 	m_item_sizer->Add(
-		new wxStaticText(this, wxID_ANY, wxGetTranslation(wxString::FromUTF8(WiimoteController::get_button_name(button_id)))),
+		new wxStaticText(this, wxID_ANY, wxGetTranslation(wxString::FromUTF8(WiimoteController::get_button_name(button_id).data(), WiimoteController::get_button_name(button_id).size()))),
 		wxGBPosition(row, column),
 		wxDefaultSpan,
 		wxALL | wxALIGN_CENTER_VERTICAL, 5);
diff --git i/src/gui/wxgui/wxHelper.h w/src/gui/wxgui/wxHelper.h
index 9bbaddac..2fac9b68 100644
--- i/src/gui/wxgui/wxHelper.h
+++ w/src/gui/wxgui/wxHelper.h
@@ -2,6 +2,8 @@
 #include <wx/string.h>
 #include <wx/bitmap.h>
 #include <wx/mstream.h>
+#include <wx/settings.h>
+#include <wx/image.h>
 
 namespace wxHelper
 {
@@ -36,4 +38,14 @@ namespace wxHelper
     	img.Replace(0x00, 0x00, 0x00, tint.Red(), tint.Green(), tint.Blue());
     	return wxBitmap(img);
     }
+
+	template<typename T>
+	static T SelectLightDark(const T& light, const T& dark)
+	{
+#if wxCHECK_VERSION(3, 3, 0)
+		return wxSystemSettings::SelectLightDark(light, dark);
+#else
+		return wxSystemSettings::GetAppearance().IsDark() ? dark : light;
+#endif
+	}
 };
diff --git i/src/gui/wxgui/wxWindowSystem.cpp w/src/gui/wxgui/wxWindowSystem.cpp
index bb730db8..3e4dfb57 100644
--- i/src/gui/wxgui/wxWindowSystem.cpp
+++ w/src/gui/wxgui/wxWindowSystem.cpp
@@ -62,8 +62,8 @@ void WindowSystem::ShowErrorDialog(std::string_view message, std::string_view ti
 	if (title.empty())
 		caption = wxASCII_STR(wxMessageBoxCaptionStr);
 	else
-		caption = wxString::FromUTF8(title);
-	wxMessageBox(wxString::FromUTF8(message), caption, wxOK | wxCENTRE | wxICON_ERROR);
+		caption = wxString::FromUTF8(title.data(), title.size());
+	wxMessageBox(wxString::FromUTF8(message.data(), message.size()), caption, wxOK | wxCENTRE | wxICON_ERROR);
 }
 
 WindowSystem::WindowInfo& WindowSystem::GetWindowInfo()
diff --git i/src/gui/wxgui/wxgui.h w/src/gui/wxgui/wxgui.h
index bb4352d1..f0175ba7 100644
--- i/src/gui/wxgui/wxgui.h
+++ w/src/gui/wxgui/wxgui.h
@@ -35,6 +35,7 @@
 #include <wx/wupdlock.h>
 #include <wx/infobar.h>
 #include <wx/splitter.h>
+#include "wxHelper.h"
 
 extern bool g_inputConfigWindowHasFocus;
 
